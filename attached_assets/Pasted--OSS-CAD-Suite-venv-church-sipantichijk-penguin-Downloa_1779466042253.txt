⦗OSS CAD Suite⦘ (venv-church) sipantichijk@penguin:~/Downloads/Ti60/church-ti60-package$ # Check if timing builder patch applied
grep -n "2025.2 patch: CLK" \
  /home/sipantichijk/efinity/2025.2/pt/bin/api_service/unified_netlist/peri_timing_netlist_builder.py

# Show all four hsio_device_service call sites
sed -n '1065,1080p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/hsio/hsio_device_service.py
echo "---"
sed -n '1316,1332p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/hsio/hsio_device_service.py
echo "---"
sed -n '1381,1398p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/hsio/hsio_device_service.py
echo "---"
sed -n '1417,1432p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/hsio/hsio_device_service.py
98:    if device_ins is None:  # 2025.2 patch: CLK/CDI resources not in device db

        mode_type = None

        dev_ins = device_db.find_instance(dev_ins_name)
        if dev_ins is not None:

            # Get the user resources
            mode2resource_map = dev_ins.get_mode_to_resource_map()
            for mode, resource in mode2resource_map.items():
                if resource == dev_ins_name:
                    mode_type = self.str2mode_map[mode]
                    break

        return mode_type

    def get_gpio_pad_type(self, res_name):
---
            res_name = ""
            if ins_obj.gpio_def != "":
                res_name = ins_obj.gpio_def
            else:
                # Check the dev_ins_name
                res_name = dev_ins_name

            mode2resource_map = dev_ins.get_mode_to_resource_map()
            for mode, resource in mode2resource_map.items():
                self.logger.debug("Reading mode: {} resource: {}".format(
                    mode, resource))

                if resource == res_name:
                    mode_type = self.str2mode_map[mode]

                    if mode_type == self.HSIOBlockModeType.lvttl1 or \
                            mode_type == self.HSIOBlockModeType.gpiox4p:
---
        elif ins_obj.ops_type == ins_obj.OpsType.op_rx:
            is_rx = True
        else:
            # Bidir
            is_tx = True
            is_rx = True

        mode2resource_map = dev_ins.get_mode_to_resource_map()
        for mode, resource in mode2resource_map.items():
            if resource == res_name:
                mode_type = self.str2mode_map[mode]

                if mode_type == self.HSIOBlockModeType.lvds_rx and\
                        not is_tx and is_rx:
                    hsio_mode = self.MODE_LVDS_RX
                elif mode_type == self.HSIOBlockModeType.lvds_tx and\
                        is_tx and not is_rx:
                    hsio_mode = self.MODE_LVDS_TX
---

        # Check the MIPI DPHY type
        is_tx = False

        if ins_obj.ops_type == MIPIDPhy.OpsType.op_tx:
            is_tx = True

        mode2resource_map = dev_ins.get_mode_to_resource_map()
        for mode, resource in mode2resource_map.items():
            if resource == res_name:
                mode_type = self.str2mode_map[mode]

                if mode_type == self.HSIOBlockModeType.mipi_dphy_rx and not is_tx:
                    hsio_mode = self.MODE_MIPI_DPHY_RX
                elif mode_type == self.HSIOBlockModeType.mipi_dphy_tx and is_tx:
                    hsio_mode = self.MODE_MIPI_DPHY_TX
⦗OSS CAD Suite⦘ (venv-church) sipantichijk@penguin:~/Downloads/Ti60/church-ti60-package$ 